'use strict';

const test = 'alabama';

function permute(input, left, right) {
  const result = [];

  if (input.length === 1) {
    result.push(input[0]);
    return result;
  }

  for (let i = 0; i < input.length; i++) {
    const char = input[i];

  }

}